# Next Steps

Current status: local simulator v7 is uploaded. This v8 patch adds data-quality summary and readable source drawer UI.

## Immediate next steps after patch upload

1. Run the test suite locally.
2. Start the local server and verify the UI.
3. Confirm `/api/data-quality` works.
4. Confirm the readable source drawer opens from result cards.
5. Build the internal admin verification queue next.

## Do not do

- Do not publish hidden rows by default.
- Do not change college data values without source verification.
- Do not merge MHT-CET and JEE rank logic.
- Do not claim guaranteed admission, job, branch, or package.
